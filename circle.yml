machine:
  timezone: Europe/London
  node:
    version: 6.10.3

test:
  post:
    - bash <(curl -s https://codecov.io/bash)

deployment:
  lambda:
    branch: master
    commands:
      - make deploy

general:
  artifacts:
    - coverage
